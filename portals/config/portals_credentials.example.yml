# ─────────────────────────────────────────────────────────────
# BalfeGo Scraper — Portal Credentials & Sensitive Parameters
# Copy this file to 'portals_credentials.yml' and fill in real values.
# 'portals_credentials.yml' is ignored by git.
# ─────────────────────────────────────────────────────────────

messer:
  username: "your_messer_username"
  password: "your_messer_password"
  # Optional portal-specific extra sensitive parameters:
  # api_key: "optional_api_key"

carburos_metalicos:
  username: "your_carburos_username"
  password: "your_carburos_password"
  # Optional portal-specific extra sensitive parameters:
  # client_id: "optional_client_id"
